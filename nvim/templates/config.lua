local config = {}


-- settings that need to be set before core loads
config.init = {
default_background = "dark",
dark_theme = 'gruvbox',
light_theme = 'default',
}


-- settings that overide core/module settings
config.config = {
}


Config.user_config = config
